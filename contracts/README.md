# Contracts

`openapi.yaml` describes the JSON API used by the Vue SPA.

When request or response shapes change, update:

- `contracts/openapi.yaml`
- backend requests/resources
- frontend `src/shared/api/types`
