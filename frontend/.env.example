# Docker compose reads frontend variables from the root .env file.
# Keep this file only for running frontend standalone without Docker.
VITE_API_URL=/api
VITE_BACKEND_URL=
VITE_DEV_PROXY_TARGET=http://localhost:25200
VITE_ALLOWED_HOSTS=localhost,127.0.0.1,phpvue.sborkaweb.ru
VITE_PORT=25300
